<Page x:Class="Lemoo.Modules.TaskManager.UI.Views.Pages.TaskListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="任务列表">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 工具栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBox Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}" 
                     Width="200" 
                     Margin="0,0,10,0"
                     VerticalContentAlignment="Center"/>
            <Button Content="搜索" Command="{Binding SearchCommand}" Margin="0,0,10,0"/>
            <Button Content="刷新" Command="{Binding LoadTasksCommand}" Margin="0,0,10,0"/>
            <Button Content="新建任务" Command="{Binding CreateTaskCommand}"/>
        </StackPanel>
        
        <!-- 任务列表 -->
        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding Tasks}" 
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="标题" Binding="{Binding Title}" Width="200"/>
                <DataGridTextColumn Header="描述" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="优先级" Binding="{Binding PriorityName}" Width="80"/>
                <DataGridTextColumn Header="状态" Binding="{Binding StatusName}" Width="80"/>
                <DataGridTextColumn Header="截止日期" Binding="{Binding DueDate, StringFormat='{}{0:yyyy-MM-dd}'}" Width="120"/>
                <DataGridTemplateColumn Header="操作" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="完成" 
                                        Command="{Binding DataContext.CompleteTaskCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Margin="5,0"/>
                                <Button Content="删除" 
                                        Command="{Binding DataContext.DeleteTaskCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Margin="5,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- 加载遮罩 -->
        <Border Grid.Row="1" 
                Background="#80000000" 
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock Text="加载中..." 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"
                       Foreground="White"
                       FontSize="16"/>
        </Border>
    </Grid>
</Page>
