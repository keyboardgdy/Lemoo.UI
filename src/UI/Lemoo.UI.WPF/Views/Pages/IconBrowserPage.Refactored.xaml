<Page x:Class="Lemoo.UI.WPF.Views.Pages.IconBrowserPageRefactored"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:icons="clr-namespace:Lemoo.UI.Controls.Icons;assembly=Lemoo.UI"
      xmlns:converters="clr-namespace:Lemoo.UI.WPF.Converters"
      xmlns:viewmodels="clr-namespace:Lemoo.UI.WPF.ViewModels.Pages"
      xmlns:layout="clr-namespace:Lemoo.UI.Controls.Layout;assembly=Lemoo.UI"
      mc:Ignorable="d"
      d:DesignWidth="1280"
      d:DesignHeight="720"
      Background="Transparent"
      d:DataContext="{d:DesignInstance Type=viewmodels:IconBrowserPageViewModelRefactored, IsDesignTimeCreatable=False}">

    <Page.Resources>
        <!-- 合并样式资源 -->
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Lemoo.UI.WPF;component/Styles/IconBrowserPageStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0"
                   Text="图标浏览器"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource InputForegroundBrush}"
                   Margin="0,0,0,16" />

        <!-- 搜索和分类栏 -->
        <Border Grid.Row="1"
                Style="{StaticResource DetailPanelBorderStyle}"
                Padding="8"
                Margin="0,0,0,16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- 搜索框和尺寸选择 -->
                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- 搜索框 -->
                    <Border Grid.Column="0"
                            Background="{DynamicResource InputBackgroundBrush}"
                            BorderBrush="{DynamicResource InputBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="6"
                            MinWidth="300">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!-- 搜索图标 -->
                            <icons:Icon Grid.Column="0"
                                        IconKind="Search"
                                        IconSize="Small"
                                        Foreground="{DynamicResource InputForegroundBrush}"
                                        Opacity="0.5"
                                        VerticalAlignment="Center"
                                        Margin="12,0,8,0" />

                            <!-- 输入框 -->
                            <TextBox x:Name="SearchTextBox"
                                     Grid.Column="1"
                                     Style="{StaticResource IconBrowserSearchBox}"
                                     Tag="搜索图标..."
                                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=300}">
                                <TextBox.InputBindings>
                                    <KeyBinding Key="Escape" Command="{Binding ClearSearchCommand}" />
                                </TextBox.InputBindings>
                            </TextBox>
                        </Grid>
                    </Border>

                    <!-- 尺寸选择 -->
                    <StackPanel Grid.Column="1"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Margin="16,0,0,0">
                        <TextBlock Text="图标尺寸:"
                                   Foreground="{DynamicResource InputForegroundBrush}"
                                   Opacity="0.7"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0" />
                        <RadioButton Style="{StaticResource SizeRadioButtonStyle}"
                                     Content="XS"
                                     GroupName="IconSize"
                                     Tag="ExtraSmall"
                                     IsChecked="{Binding CurrentIconSize, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=ExtraSmall}" />
                        <RadioButton Style="{StaticResource SizeRadioButtonStyle}"
                                     Content="S"
                                     GroupName="IconSize"
                                     Tag="Small"
                                     IsChecked="{Binding CurrentIconSize, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=Small}" />
                        <RadioButton Style="{StaticResource SizeRadioButtonStyle}"
                                     Content="M"
                                     GroupName="IconSize"
                                     Tag="Normal"
                                     IsChecked="{Binding CurrentIconSize, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=Normal}" />
                        <RadioButton Style="{StaticResource SizeRadioButtonStyle}"
                                     Content="L"
                                     GroupName="IconSize"
                                     Tag="Medium"
                                     IsChecked="{Binding CurrentIconSize, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=Medium}" />
                        <RadioButton Style="{StaticResource SizeRadioButtonStyle}"
                                     Content="XL"
                                     GroupName="IconSize"
                                     Tag="Large"
                                     IsChecked="{Binding CurrentIconSize, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=Large}" />
                    </StackPanel>
                </Grid>

                <!-- 分类标签 -->
                <ScrollViewer Grid.Row="1"
                              HorizontalScrollBarVisibility="Auto"
                              VerticalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding Categories}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type viewmodels:IconCategoryItemViewModel}">
                                <RadioButton Style="{StaticResource CategoryRadioButtonStyle}"
                                             Content="{Binding DisplayName}"
                                             GroupName="Category"
                                             IsChecked="{Binding IsSelected}"
                                             Command="{Binding DataContext.SelectCategoryCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                             CommandParameter="{Binding}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- 主内容区域：图标列表 + 详情面板 -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="380" />
            </Grid.ColumnDefinitions>

            <!-- 图标网格（带虚拟化支持） -->
            <Border Grid.Column="0"
                    Style="{StaticResource DetailPanelBorderStyle}"
                    Padding="16">
                <ListBox x:Name="IconListBox"
                         Style="{StaticResource IconListBoxStyle}"
                         ItemsSource="{Binding FilteredIcons}"
                         SelectedItem="{Binding SelectedIcon}"
                         ItemContainerStyle="{StaticResource IconListBoxItemStyle}">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <layout:VirtualizingWrapPanel ChildWidth="100" ChildHeight="100"
                                                        ItemHorizontalSpacing="6"
                                                        ItemVerticalSpacing="6" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type viewmodels:IconItemViewModel}">
                            <Border Width="100" Height="100"
                                    Style="{StaticResource IconGridItemContainerStyle}">
                                <Grid>
                                    <icons:Icon IconKind="{Binding Kind}"
                                                IconSize="Normal"
                                                Foreground="{DynamicResource InputForegroundBrush}"
                                                VerticalAlignment="Top"
                                                HorizontalAlignment="Center"
                                                Margin="0,8,0,0" />
                                    <TextBlock Text="{Binding Name}"
                                               FontSize="10"
                                               Foreground="{DynamicResource InputForegroundBrush}"
                                               Opacity="0.7"
                                               TextAlignment="Center"
                                               TextTrimming="CharacterEllipsis"
                                               VerticalAlignment="Bottom"
                                               Margin="6,0,6,6" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <!-- 图标详情面板 -->
            <Border Grid.Column="2" Style="{StaticResource DetailPanelBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- 标题栏 -->
                    <Border Grid.Row="0"
                            BorderBrush="{DynamicResource InputBorderBrush}"
                            BorderThickness="0,0,0,1"
                            Padding="0,12,0,12">
                        <TextBlock Text="图标详情"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource InputForegroundBrush}" />
                    </Border>

                    <!-- 内容区域 -->
                    <ScrollViewer Grid.Row="1"
                                  VerticalScrollBarVisibility="Auto"
                                  Padding="0,16,0,16">
                        <StackPanel>
                            <!-- 空状态提示 -->
                            <StackPanel x:Name="EmptyState"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Visibility="{Binding HasSelectedIcon, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                                <icons:Icon IconKind="Info"
                                            IconSize="Medium"
                                            Foreground="{DynamicResource InputForegroundBrush}"
                                            Opacity="0.4"
                                            HorizontalAlignment="Center"
                                            Margin="0,0,0,8" />
                                <TextBlock Text="点击左侧图标查看详情"
                                           Foreground="{DynamicResource InputForegroundBrush}"
                                           Opacity="0.5"
                                           FontSize="12"
                                           HorizontalAlignment="Center" />
                            </StackPanel>

                            <!-- 图标详情内容 -->
                            <StackPanel Visibility="{Binding HasSelectedIcon, Converter={StaticResource BoolToVisibilityConverter}}">
                                <!-- 图标预览区 -->
                                <Border Background="{DynamicResource InputBackgroundBrush}"
                                        BorderBrush="{DynamicResource InputBorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Padding="20"
                                        Margin="0,0,0,16">
                                    <icons:Icon IconKind="{Binding SelectedIcon.Kind}"
                                                IconSize="ExtraLarge"
                                                Foreground="{DynamicResource AccentPrimaryBrush}"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center" />
                                </Border>

                                <!-- 尺寸预览 -->
                                <Border Background="{DynamicResource InputBackgroundBrush}"
                                        BorderBrush="{DynamicResource InputBorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Padding="12"
                                        Margin="0,0,0,16">
                                    <StackPanel>
                                        <TextBlock Text="尺寸预览"
                                                   Foreground="{DynamicResource InputForegroundBrush}"
                                                   Opacity="0.7"
                                                   FontSize="11"
                                                   Margin="0,0,0,8" />
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0" Margin="4">
                                                <TextBlock Text="XS"
                                                           Foreground="{DynamicResource InputForegroundBrush}"
                                                           Opacity="0.5"
                                                           FontSize="10"
                                                           HorizontalAlignment="Center"
                                                           Margin="0,0,0,4" />
                                                <Border Background="{DynamicResource WorkbenchSurfaceBrush}"
                                                        CornerRadius="4"
                                                        Padding="8"
                                                        HorizontalAlignment="Center">
                                                    <icons:Icon IconKind="{Binding SelectedIcon.Kind}"
                                                                IconSize="ExtraSmall"
                                                                Foreground="{DynamicResource InputForegroundBrush}" />
                                                </Border>
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" Margin="4">
                                                <TextBlock Text="S"
                                                           Foreground="{DynamicResource InputForegroundBrush}"
                                                           Opacity="0.5"
                                                           FontSize="10"
                                                           HorizontalAlignment="Center"
                                                           Margin="0,0,0,4" />
                                                <Border Background="{DynamicResource WorkbenchSurfaceBrush}"
                                                        CornerRadius="4"
                                                        Padding="8"
                                                        HorizontalAlignment="Center">
                                                    <icons:Icon IconKind="{Binding SelectedIcon.Kind}"
                                                                IconSize="Small"
                                                                Foreground="{DynamicResource InputForegroundBrush}" />
                                                </Border>
                                            </StackPanel>
                                            <StackPanel Grid.Column="2" Margin="4">
                                                <TextBlock Text="M"
                                                           Foreground="{DynamicResource InputForegroundBrush}"
                                                           Opacity="0.5"
                                                           FontSize="10"
                                                           HorizontalAlignment="Center"
                                                           Margin="0,0,0,4" />
                                                <Border Background="{DynamicResource WorkbenchSurfaceBrush}"
                                                        CornerRadius="4"
                                                        Padding="8"
                                                        HorizontalAlignment="Center">
                                                    <icons:Icon IconKind="{Binding SelectedIcon.Kind}"
                                                                IconSize="Normal"
                                                                Foreground="{DynamicResource InputForegroundBrush}" />
                                                </Border>
                                            </StackPanel>
                                            <StackPanel Grid.Column="3" Margin="4">
                                                <TextBlock Text="L"
                                                           Foreground="{DynamicResource InputForegroundBrush}"
                                                           Opacity="0.5"
                                                           FontSize="10"
                                                           HorizontalAlignment="Center"
                                                           Margin="0,0,0,4" />
                                                <Border Background="{DynamicResource WorkbenchSurfaceBrush}"
                                                        CornerRadius="4"
                                                        Padding="8"
                                                        HorizontalAlignment="Center">
                                                    <icons:Icon IconKind="{Binding SelectedIcon.Kind}"
                                                                IconSize="Medium"
                                                                Foreground="{DynamicResource InputForegroundBrush}" />
                                                </Border>
                                            </StackPanel>
                                        </Grid>
                                    </StackPanel>
                                </Border>

                                <!-- 颜色预览 -->
                                <Border Background="{DynamicResource InputBackgroundBrush}"
                                        BorderBrush="{DynamicResource InputBorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Padding="12"
                                        Margin="0,0,0,16">
                                    <StackPanel>
                                        <TextBlock Text="颜色预览"
                                                   Foreground="{DynamicResource InputForegroundBrush}"
                                                   Opacity="0.7"
                                                   FontSize="11"
                                                   Margin="0,0,0,8" />
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Border Grid.Column="0"
                                                    Background="#FF212121"
                                                    CornerRadius="4"
                                                    Padding="12"
                                                    Margin="4">
                                                <icons:Icon IconKind="{Binding SelectedIcon.Kind}"
                                                            IconSize="Medium"
                                                            Foreground="#FFFFFFFF" />
                                            </Border>
                                            <Border Grid.Column="1"
                                                    Background="{DynamicResource AccentPrimaryBrush}"
                                                    CornerRadius="4"
                                                    Padding="12"
                                                    Margin="4">
                                                <icons:Icon IconKind="{Binding SelectedIcon.Kind}"
                                                            IconSize="Medium"
                                                            Foreground="{DynamicResource OnAccentForegroundBrush}" />
                                            </Border>
                                            <Border Grid.Column="2"
                                                    Background="#FFFFFFFF"
                                                    BorderBrush="{DynamicResource InputBorderBrush}"
                                                    BorderThickness="1"
                                                    CornerRadius="4"
                                                    Padding="12"
                                                    Margin="4">
                                                <icons:Icon IconKind="{Binding SelectedIcon.Kind}"
                                                            IconSize="Medium"
                                                            Foreground="#FF212121" />
                                            </Border>
                                        </Grid>
                                    </StackPanel>
                                </Border>

                                <!-- 图标信息 -->
                                <StackPanel>
                                    <!-- 图标名称 -->
                                    <StackPanel Margin="0,0,0,12">
                                        <TextBlock Text="名称"
                                                   Foreground="{DynamicResource InputForegroundBrush}"
                                                   Opacity="0.6"
                                                   FontSize="11"
                                                   Margin="0,0,0,4" />
                                        <TextBlock Text="{Binding DetailName}"
                                                   Foreground="{DynamicResource InputForegroundBrush}"
                                                   FontSize="14"
                                                   FontWeight="SemiBold" />
                                    </StackPanel>

                                    <!-- 图标分类 -->
                                    <StackPanel Margin="0,0,0,12">
                                        <TextBlock Text="分类"
                                                   Foreground="{DynamicResource InputForegroundBrush}"
                                                   Opacity="0.6"
                                                   FontSize="11"
                                                   Margin="0,0,0,4" />
                                        <TextBlock Text="{Binding DetailCategory}"
                                                   Foreground="{DynamicResource InputForegroundBrush}"
                                                   FontSize="13" />
                                    </StackPanel>

                                    <!-- 图标代码 -->
                                    <StackPanel Margin="0,0,0,12">
                                        <TextBlock Text="图标代码"
                                                   Foreground="{DynamicResource InputForegroundBrush}"
                                                   Opacity="0.6"
                                                   FontSize="11"
                                                   Margin="0,0,0,4" />
                                        <Grid>
                                            <Border Style="{StaticResource DetailItemBorderStyle}">
                                                <TextBlock Text="{Binding DetailCode}"
                                                           Foreground="{DynamicResource InputForegroundBrush}"
                                                           FontFamily="Consolas"
                                                           FontSize="12" />
                                            </Border>
                                            <Button Style="{StaticResource CopyButtonStyle}"
                                                    Command="{Binding CopyDetailCommand}"
                                                    CommandParameter="Code"
                                                    Margin="0,0,4,0"
                                                    HorizontalAlignment="Right"
                                                    VerticalAlignment="Center">
                                                <icons:Icon IconKind="Copy"
                                                            IconSize="ExtraSmall"
                                                            Foreground="{DynamicResource InputForegroundBrush}" />
                                            </Button>
                                        </Grid>
                                    </StackPanel>

                                    <!-- Unicode -->
                                    <StackPanel Margin="0,0,0,12">
                                        <TextBlock Text="Unicode"
                                                   Foreground="{DynamicResource InputForegroundBrush}"
                                                   Opacity="0.6"
                                                   FontSize="11"
                                                   Margin="0,0,0,4" />
                                        <Grid>
                                            <Border Style="{StaticResource DetailItemBorderStyle}">
                                                <TextBlock Text="{Binding DetailUnicode}"
                                                           Foreground="{DynamicResource InputForegroundBrush}"
                                                           FontFamily="Consolas"
                                                           FontSize="12" />
                                            </Border>
                                            <Button Style="{StaticResource CopyButtonStyle}"
                                                    Command="{Binding CopyDetailCommand}"
                                                    CommandParameter="Unicode"
                                                    Margin="0,0,4,0"
                                                    HorizontalAlignment="Right"
                                                    VerticalAlignment="Center">
                                                <icons:Icon IconKind="Copy"
                                                            IconSize="ExtraSmall"
                                                            Foreground="{DynamicResource InputForegroundBrush}" />
                                            </Button>
                                        </Grid>
                                    </StackPanel>

                                    <!-- 字形 -->
                                    <StackPanel Margin="0,0,0,12">
                                        <TextBlock Text="字形"
                                                   Foreground="{DynamicResource InputForegroundBrush}"
                                                   Opacity="0.6"
                                                   FontSize="11"
                                                   Margin="0,0,0,4" />
                                        <Grid>
                                            <Border Style="{StaticResource DetailItemBorderStyle}">
                                                <TextBlock Text="{Binding DetailGlyph}"
                                                           Foreground="{DynamicResource InputForegroundBrush}"
                                                           FontFamily="Consolas"
                                                           FontSize="12" />
                                            </Border>
                                            <Button Style="{StaticResource CopyButtonStyle}"
                                                    Command="{Binding CopyDetailCommand}"
                                                    CommandParameter="Glyph"
                                                    Margin="0,0,4,0"
                                                    HorizontalAlignment="Right"
                                                    VerticalAlignment="Center">
                                                <icons:Icon IconKind="Copy"
                                                            IconSize="ExtraSmall"
                                                            Foreground="{DynamicResource InputForegroundBrush}" />
                                            </Button>
                                        </Grid>
                                    </StackPanel>

                                    <!-- 关键词 -->
                                    <StackPanel>
                                        <TextBlock Text="关键词"
                                                   Foreground="{DynamicResource InputForegroundBrush}"
                                                   Opacity="0.6"
                                                   FontSize="11"
                                                   Margin="0,0,0,4" />
                                        <ItemsControl ItemsSource="{Binding SelectedIcon.IconInfo.Keywords}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Horizontal" />
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Style="{StaticResource KeywordTagBorderStyle}">
                                                        <TextBlock Text="{Binding}"
                                                                   Foreground="{DynamicResource InputForegroundBrush}"
                                                                   FontSize="11" />
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- 统计信息 - 右下角 -->
        <TextBlock Grid.Row="2"
                   Foreground="{DynamicResource InputForegroundBrush}"
                   Opacity="0.7"
                   FontSize="12"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Bottom"
                   Margin="0,0,0,12">
            <Run Text="总计" />
            <Run Text="·" />
            <Run Text="{Binding TotalIconCount, Mode=OneWay}" FontWeight="SemiBold" />
            <Run Text="个图标" />
            <Run Text="·" />
            <Run Text="{Binding DisplayedIconCount, Mode=OneWay}" FontWeight="SemiBold" />
            <Run Text="个显示" />
        </TextBlock>

        <!-- 通知提示 -->
        <Border Grid.Row="2"
                Style="{StaticResource NotificationBorderStyle}"
                Visibility="{Binding Source={x:Static Application.Current}, Path=Resources.NotificationService.IsVisible, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="{Binding Source={x:Static Application.Current}, Path=Resources.NotificationService.CurrentMessage}"
                       Foreground="{DynamicResource OnAccentForegroundBrush}"
                       FontSize="13"
                       FontWeight="Medium"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center">
                <TextBlock.Style>
                    <Style>
                        <Style.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                       From="0" To="1" Duration="0:0:0.3">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Border>
    </Grid>
</Page>
