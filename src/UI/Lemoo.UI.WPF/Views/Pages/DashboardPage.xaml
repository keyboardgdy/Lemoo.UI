<Page x:Class="Lemoo.UI.WPF.Views.Pages.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="Transparent">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="仪表盘（示例页面）"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource MenuForegroundBrush}" />

        <TextBlock Grid.Row="1"
                   Margin="0,8,0,16"
                   Text="这里是一个简单的仪表盘示例，用于测试导航和标签页功能。"
                   Foreground="{DynamicResource InputForegroundBrush}" />

        <Border Grid.Row="2"
                Background="{DynamicResource WorkbenchCardBrush}"
                CornerRadius="8"
                Padding="16">
            <StackPanel>
                <TextBlock Text="快速统计"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource MenuForegroundBrush}"
                           Margin="0,0,0,8" />

                <UniformGrid Columns="3" Margin="0,8,0,0">
                    <Border Background="{DynamicResource AccentPrimaryBrush}" CornerRadius="6" Margin="0,0,12,0" Padding="12">
                        <StackPanel>
                            <TextBlock Text="示例数量" Foreground="{DynamicResource MenuForegroundBrush}" FontSize="12" />
                            <TextBlock Text="42" Foreground="{DynamicResource MenuForegroundBrush}" FontSize="20" FontWeight="Bold" />
                        </StackPanel>
                    </Border>
                    <Border Background="{DynamicResource WorkbenchSurfaceBrush}" CornerRadius="6" Margin="0,0,12,0" Padding="12">
                        <StackPanel>
                            <TextBlock Text="在线用户" Foreground="{DynamicResource MenuForegroundBrush}" FontSize="12" />
                            <TextBlock Text="7" Foreground="{DynamicResource MenuForegroundBrush}" FontSize="20" FontWeight="Bold" />
                        </StackPanel>
                    </Border>
                    <Border Background="{DynamicResource WorkbenchBackgroundBrush}" CornerRadius="6" Padding="12">
                        <StackPanel>
                            <TextBlock Text="系统状态" Foreground="{DynamicResource MenuForegroundBrush}" FontSize="12" />
                            <TextBlock Text="正常" Foreground="{DynamicResource AccentPrimaryBrush}" FontSize="20" FontWeight="Bold" />
                        </StackPanel>
                    </Border>
                </UniformGrid>
            </StackPanel>
        </Border>
    </Grid>
</Page>


