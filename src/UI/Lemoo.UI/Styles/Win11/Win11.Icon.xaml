<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:icons="clr-namespace:Lemoo.UI.Controls.Icons">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Lemoo.UI;component/Converters/Converters.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!-- ==================== Icon 控件样式 ==================== -->

    <Style TargetType="{x:Type icons:Icon}">
        <Setter Property="FontFamily" Value="{DynamicResource FontFamily.Icon}" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="RenderOptions.ClearTypeHint" Value="Enabled" />
        <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
        <Setter Property="TextOptions.TextRenderingMode" Value="Auto" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type icons:Icon}">
                    <TextBlock x:Name="IconTextBlock"
                               Text="{TemplateBinding IconKind, Converter={StaticResource IconKindToGlyphConverter}}"
                               FontFamily="{TemplateBinding FontFamily}"
                               FontSize="{TemplateBinding IconSize, Converter={StaticResource IconSizeToFontSizeConverter}}"
                               Foreground="{TemplateBinding Foreground}"
                               HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                               VerticalAlignment="{TemplateBinding VerticalAlignment}"
                               RenderOptions.ClearTypeHint="Enabled"
                               TextOptions.TextFormattingMode="Display"
                               TextOptions.TextRenderingMode="Auto" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ==================== IconButton 控件样式 ==================== -->

    <!-- Primary IconButton -->
    <Style x:Key="IconButton.Primary" TargetType="{x:Type icons:IconButton}">
        <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource TextOnAccentBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="12" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type icons:IconButton}">
                    <Border x:Name="RootBorder"
                            Background="{TemplateBinding Background}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            Padding="{TemplateBinding Padding}"
                            CornerRadius="4"
                            SnapsToDevicePixels="True">
                        <icons:Icon IconKind="{TemplateBinding IconKind}"
                                    IconSize="{TemplateBinding IconSize}"
                                    IconVariant="{TemplateBinding IconVariant}"
                                    Foreground="{TemplateBinding Foreground}"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource AccentBrushHover}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource AccentBrushPressed}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="RootBorder" Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Secondary IconButton -->
    <Style x:Key="IconButton.Secondary" TargetType="{x:Type icons:IconButton}" BasedOn="{StaticResource IconButton.Primary}">
        <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource ControlFillColorTertiaryBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource ControlFillColorQuarternaryBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Outline IconButton -->
    <Style x:Key="IconButton.Outline" TargetType="{x:Type icons:IconButton}" BasedOn="{StaticResource IconButton.Primary}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeBrush}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource ControlFillColorTertiaryBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Ghost IconButton -->
    <Style x:Key="IconButton.Ghost" TargetType="{x:Type icons:IconButton}" BasedOn="{StaticResource IconButton.Primary}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource ControlFillColorTertiaryBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Default IconButton Style (uses Primary) -->
    <Style TargetType="{x:Type icons:IconButton}" BasedOn="{StaticResource IconButton.Primary}" />

    <!-- ==================== IconBadge 控件样式 ==================== -->

    <Style TargetType="{x:Type icons:IconBadge}">
        <Setter Property="FontFamily" Value="{DynamicResource FontFamily.Icon}" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="BadgeBackground" Value="{DynamicResource SystemAttentionBackgroundBrush}" />
        <Setter Property="BadgeForeground" Value="{DynamicResource TextOnAccentBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type icons:IconBadge}">
                    <Grid>
                        <!-- Icon -->
                        <TextBlock x:Name="IconTextBlock"
                                   Text="{TemplateBinding IconKind, Converter={StaticResource IconKindToGlyphConverter}}"
                                   FontFamily="{TemplateBinding FontFamily}"
                                   FontSize="{TemplateBinding IconSize, Converter={StaticResource IconSizeToFontSizeConverter}}"
                                   Foreground="{TemplateBinding Foreground}"
                                   HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                                   VerticalAlignment="{TemplateBinding VerticalAlignment}"
                                   RenderOptions.ClearTypeHint="Enabled"
                                   TextOptions.TextFormattingMode="Display"
                                   TextOptions.TextRenderingMode="Auto" />

                        <!-- Badge -->
                        <Border x:Name="BadgeBorder"
                                Background="{TemplateBinding BadgeBackground}"
                                CornerRadius="8"
                                Padding="4,2"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Top"
                                Visibility="{TemplateBinding BadgeValue, Converter={StaticResource StringToVisibilityConverter}}">
                            <TextBlock Text="{TemplateBinding BadgeValue}"
                                       Foreground="{TemplateBinding BadgeForeground}"
                                       FontSize="10"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />
                        </Border>

                        <!-- Dot Badge -->
                        <Border x:Name="DotBadgeBorder"
                                Background="{TemplateBinding BadgeBackground}"
                                Width="8"
                                Height="8"
                                CornerRadius="4"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Top"
                                Visibility="{TemplateBinding ShowDot, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
