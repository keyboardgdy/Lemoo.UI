<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Lemoo.UI;component/Styles/Design/Animations.xaml"/>
        <ResourceDictionary Source="pack://application:,,,/Lemoo.UI;component/Styles/Design/Shadows.xaml"/>
        <ResourceDictionary Source="pack://application:,,,/Lemoo.UI;component/Themes/Base/ColorPalette.xaml"/>
        <ResourceDictionary Source="pack://application:,,,/Lemoo.UI;component/Themes/Base/SemanticTokens.xaml"/>
        <ResourceDictionary Source="pack://application:,,,/Lemoo.UI;component/Styles/Win11/Win11.Tokens.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- ============================================
         MenuItem 样式 (Win11)
         ============================================ -->

    <!-- 顶级菜单项样式 -->
    <Style x:Key="Win11.MenuItem.TopLevel" TargetType="MenuItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource MenuItemForegroundBrush}"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="FontSize" Value="{StaticResource FontSize.Normal}"/>
        <Setter Property="FontFamily" Value="{StaticResource FontFamily.Default}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}"
                            CornerRadius="{StaticResource Win11.CornerRadius.Small}"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <!-- 图标 -->
                            <ContentPresenter x:Name="icon"
                                            Grid.Column="0"
                                            ContentSource="Icon"
                                            Margin="0,0,8,0"
                                            VerticalAlignment="Center"/>
                            <!-- 标题 -->
                            <ContentPresenter x:Name="header"
                                            Grid.Column="1"
                                            ContentSource="Header"
                                            RecognizesAccessKey="True"
                                            VerticalAlignment="Center"/>
                            <!-- 子菜单箭头 -->
                            <Path x:Name="SubMenuArrow"
                                  Grid.Column="2"
                                  Margin="8,0,0,0"
                                  VerticalAlignment="Center"
                                  Visibility="Collapsed"
                                  Data="M 0 0 L 4 4 L 8 0 Z"
                                  Fill="{DynamicResource MenuItemForegroundBrush}"
                                  Width="8"
                                  Height="6"
                                  RenderTransformOrigin="0.5,0.5">
                                <Path.RenderTransform>
                                    <RotateTransform Angle="-90"/>
                                </Path.RenderTransform>
                            </Path>
                            <!-- 快捷键 -->
                            <TextBlock x:Name="inputGestureText"
                                       Grid.Column="2"
                                       Text="{TemplateBinding InputGestureText}"
                                       Margin="8,0,0,0"
                                       VerticalAlignment="Center"
                                       FontSize="{StaticResource FontSize.Small}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource MenuItemHoverBackgroundBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource ButtonDisabledForegroundBrush}"/>
                        </Trigger>
                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter TargetName="SubMenuArrow" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="inputGestureText" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 子菜单项样式 -->
    <Style x:Key="Win11.MenuItem.Submenu" TargetType="MenuItem">
        <Setter Property="Background" Value="{DynamicResource MenuItemBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource MenuItemForegroundBrush}"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="FontSize" Value="{StaticResource FontSize.Normal}"/>
        <Setter Property="FontFamily" Value="{StaticResource FontFamily.Default}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True"
                            MinHeight="32">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <!-- 选中指示器 -->
                            <Border x:Name="selectionIndicator"
                                    Grid.Column="0"
                                    Width="3"
                                    Background="Transparent"
                                    Margin="0,0,8,0"
                                    CornerRadius="1.5"
                                    VerticalAlignment="Stretch"/>
                            <!-- 图标 -->
                            <ContentPresenter x:Name="icon"
                                            Grid.Column="0"
                                            ContentSource="Icon"
                                            Width="16"
                                            Height="16"
                                            Margin="8,0,8,0"
                                            VerticalAlignment="Center"/>
                            <!-- 标题 -->
                            <ContentPresenter x:Name="header"
                                            Grid.Column="1"
                                            ContentSource="Header"
                                            RecognizesAccessKey="True"
                                            VerticalAlignment="Center"/>
                            <!-- 子菜单箭头 -->
                            <Path x:Name="SubMenuArrow"
                                  Grid.Column="2"
                                  Margin="8,0,0,0"
                                  VerticalAlignment="Center"
                                  Visibility="Collapsed"
                                  Data="M 0 0 L 4 4 L 8 0 Z"
                                  Fill="{DynamicResource MenuItemForegroundBrush}"
                                  Width="8"
                                  Height="6"
                                  RenderTransformOrigin="0.5,0.5">
                                <Path.RenderTransform>
                                    <RotateTransform Angle="-90"/>
                                </Path.RenderTransform>
                            </Path>
                            <!-- 快捷键 -->
                            <TextBlock x:Name="inputGestureText"
                                       Grid.Column="2"
                                       Text="{TemplateBinding InputGestureText}"
                                       Margin="24,0,0,0"
                                       VerticalAlignment="Center"
                                       FontSize="{StaticResource FontSize.Small}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"/>
                            <!-- 复选框 -->
                            <Grid x:Name="checkPanel"
                                  Grid.Column="0"
                                  Width="16"
                                  Height="16"
                                  Margin="0,0,8,0"
                                  Visibility="Collapsed"
                                  VerticalAlignment="Center">
                                <Path x:Name="checkMark"
                                      Data="M 2 6 L 5 9 L 10 3"
                                      Stroke="{DynamicResource AccentPrimaryBrush}"
                                      StrokeThickness="1.5"
                                      StrokeEndLineCap="Round"
                                      StrokeStartLineCap="Round"
                                      Visibility="Collapsed"/>
                                <Ellipse x:Name="radioMark"
                                         Fill="{DynamicResource AccentPrimaryBrush}"
                                         Width="6"
                                         Height="6"
                                         Visibility="Collapsed"/>
                            </Grid>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource MenuItemHighlightedBackgroundBrush}"/>
                            <Setter TargetName="selectionIndicator" Property="Background" Value="{DynamicResource AccentPrimaryBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource ButtonDisabledForegroundBrush}"/>
                        </Trigger>
                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter TargetName="SubMenuArrow" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="inputGestureText" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="checkPanel" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="icon" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="Role" Value="Checkable">
                            <Setter TargetName="checkPanel" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="icon" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="True"/>
                                <Condition Property="Role" Value="TopLevelHeader"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="True"/>
                                <Condition Property="Role" Value="TopLevelItem"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 菜单分隔符样式 -->
    <Style x:Key="Win11.MenuItem.Separator" TargetType="Separator">
        <Setter Property="Background" Value="{DynamicResource MenuSeparatorBrush}"/>
        <Setter Property="Height" Value="1"/>
        <Setter Property="Margin" Value="8,4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Border Background="{TemplateBinding Background}"
                            Height="{TemplateBinding Height}"
                            Margin="{TemplateBinding Margin}"
                            SnapsToDevicePixels="True"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 菜单滚动查看器样式 -->
    <Style x:Key="Win11.MenuScrollViewer" TargetType="ScrollViewer">
        <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollViewer">
                    <Grid>
                        <ScrollContentPresenter Margin="{TemplateBinding Padding}"/>
                        <ScrollBar Name="PART_VerticalScrollBar"
                                   Orientation="Vertical"
                                   DockPanel.Dock="Right"
                                   Value="{TemplateBinding VerticalOffset}"
                                   Maximum="{TemplateBinding ScrollableHeight}"
                                   ViewportSize="{TemplateBinding ViewportHeight}"
                                   Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 默认样式 - 不设置默认样式以避免冲突，用户需要显式指定样式 -->
    <!-- 如果需要默认样式，可以在使用时通过 Style 属性指定 -->

</ResourceDictionary>
