<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ui="clr-namespace:Lemoo.UI.Controls"
                    xmlns:media="clr-namespace:Lemoo.UI.Models.Media">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Lemoo.UI;component/Styles/Design/Animations.xaml"/>
        <ResourceDictionary Source="pack://application:,,,/Lemoo.UI;component/Styles/Win11/Win11.Tokens.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!-- ImageGallery 默认样式 -->
    <Style TargetType="{x:Type ui:ImageGallery}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ui:ImageGallery}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ItemsControl x:Name="PART_ItemsControl"
                                      ItemsSource="{TemplateBinding ItemsSource}"
                                      ItemTemplate="{TemplateBinding ItemTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemContainerStyle>
                                <Style TargetType="{x:Type ContentPresenter}">
                                    <Setter Property="Margin" Value="{TemplateBinding GridSpacing, RelativeSource={RelativeSource AncestorType=ui:ImageGallery}, Converter={StaticResource DoubleToThicknessConverter}}" />
                                </Style>
                            </ItemsControl.ItemContainerStyle>
                        </ItemsControl>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 图片项样式 -->
    <Style x:Key="ImageGalleryItemStyle" TargetType="{x:Type Border}">
        <Setter Property="Background" Value="{DynamicResource Brush.Card.Background}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Card.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform x:Name="ItemScale" ScaleX="1" ScaleY="1" />
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 图片项数据模板 -->
    <DataTemplate x:Key="ImageGalleryItemTemplate">
        <Border Style="{StaticResource ImageGalleryItemStyle}">
            <Grid>
                <Image Source="{Binding ImageSource}"
                       Stretch="UniformToFill"
                       RenderOptions.BitmapScalingMode="HighQuality" />
            </Grid>
        </Border>
    </DataTemplate>

    <!-- 图片预览 Popup 样式 -->
    <Style x:Key="ImagePreviewPopupStyle" TargetType="{x:Type Popup}">
        <Setter Property="Placement" Value="Center" />
        <Setter Property="AllowsTransparency" Value="True" />
        <Setter Property="PopupAnimation" Value="Fade" />
    </Style>

    <!-- 图片预览容器样式 -->
    <Style x:Key="ImagePreviewContainerStyle" TargetType="{x:Type Grid}">
        <Setter Property="Background">
            <Setter.Value>
                <SolidColorBrush Color="Black" Opacity="0.95" />
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
