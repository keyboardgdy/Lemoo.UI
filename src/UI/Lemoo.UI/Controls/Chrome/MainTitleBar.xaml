<UserControl x:Class="Lemoo.UI.Controls.Chrome.MainTitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Lemoo.UI.Controls.Chrome"
             xmlns:converters="clr-namespace:Lemoo.UI.Converters"
             mc:Ignorable="d"
             Height="32">

    <UserControl.Resources>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
        
        <!-- 菜单样式 - 现代风格 -->
        <Style TargetType="Menu">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource TitleBarForegroundBrush}" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>

        <!-- 主菜单项样式 - 现代风格 -->
        <Style TargetType="MenuItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource MenuItemForegroundBrush}" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Icon" Value="{x:Null}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Border"
                                BorderThickness="0"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}"
                                Margin="2,1"
                                Background="Transparent">
                            <Grid>
                                <ContentPresenter Content="{TemplateBinding Header}"
                                                  ContentTemplate="{TemplateBinding HeaderTemplate}"
                                                  VerticalAlignment="Center" />
                                <Popup x:Name="PART_Popup"
                                       AllowsTransparency="True"
                                       Placement="Bottom"
                                       HorizontalOffset="0"
                                       VerticalOffset="0"
                                       IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                       PopupAnimation="Fade">
                                    <Border Background="{DynamicResource MenuBackgroundBrush}"
                                            BorderBrush="{DynamicResource MenuBorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="8"
                                            Padding="2">
                                        <Border.Effect>
                                            <StaticResource ResourceKey="Shadow.Menu"/>
                                        </Border.Effect>
                                        <ItemsPresenter x:Name="ItemsPresenter"
                                                        KeyboardNavigation.DirectionalNavigation="Cycle"
                                                        Grid.IsSharedSizeScope="True"
                                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                                        KeyboardNavigation.TabNavigation="Cycle" />
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource MenuItemHoverBackgroundBrush}" />
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource MenuItemHighlightedBackgroundBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 子菜单样式 - 现代风格 -->
        <Style x:Key="SubmenuStyle" TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
            <Setter Property="Padding" Value="12,10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Border"
                                BorderThickness="0"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}"
                                Margin="2,1"
                                Background="Transparent">
                            <Grid>
                                <ContentPresenter Content="{TemplateBinding Header}"
                                                  ContentTemplate="{TemplateBinding HeaderTemplate}"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Stretch" />
                                <Popup x:Name="PART_Popup"
                                       AllowsTransparency="True"
                                       Placement="Right"
                                       HorizontalOffset="0"
                                       VerticalOffset="0"
                                       IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                       PopupAnimation="Fade">
                                    <Border Background="{DynamicResource MenuBackgroundBrush}"
                                            BorderBrush="{DynamicResource MenuBorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="8"
                                            Padding="2">
                                        <Border.Effect>
                                            <StaticResource ResourceKey="Shadow.Menu"/>
                                        </Border.Effect>
                                        <ItemsPresenter x:Name="ItemsPresenter"
                                                        KeyboardNavigation.DirectionalNavigation="Cycle"
                                                        Grid.IsSharedSizeScope="True"
                                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                                        KeyboardNavigation.TabNavigation="Cycle" />
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource MenuItemHoverBackgroundBrush}" />
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource MenuItemHighlightedBackgroundBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 分隔符样式 - 现代风格 -->
        <Style TargetType="Separator">
            <Setter Property="Background" Value="{DynamicResource MenuSeparatorBrush}" />
            <Setter Property="Height" Value="1" />
            <Setter Property="Margin" Value="10,6" />
            <Setter Property="Opacity" Value="0.5" />
        </Style>

        <!-- 弹出菜单样式 - 现代风格 -->
        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{DynamicResource MenuBackgroundBrush}"
                                BorderBrush="{DynamicResource MenuBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="2">
                            <Border.Effect>
                                <StaticResource ResourceKey="Shadow.Menu"/>
                            </Border.Effect>
                            <ItemsPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="MenuItem" BasedOn="{StaticResource SubmenuStyle}">
                        <Setter Property="Header" Value="{Binding}" />
                        <Setter Property="ItemsSource" Value="{Binding Children}" />
                        <Setter Property="HeaderTemplate">
                            <Setter.Value>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Icon}"
                                                   FontFamily="Segoe MDL2 Assets"
                                                   FontSize="14"
                                                   Foreground="{DynamicResource MenuItemForegroundBrush}"
                                                   Background="Transparent"
                                                   VerticalAlignment="Center"
                                                   Width="16"
                                                   TextAlignment="Center"
                                                   Margin="0,0,8,0"
                                                   Visibility="{Binding Icon, Converter={StaticResource StringToVisibilityConverter}}" />
                                        <TextBlock Text="{Binding Header}"
                                                   Foreground="{DynamicResource MenuItemForegroundBrush}"
                                                   Background="Transparent"
                                                   VerticalAlignment="Center" />
                                    </StackPanel>
                                </DataTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="ItemContainerStyle">
                            <Setter.Value>
                                <Style TargetType="MenuItem" BasedOn="{StaticResource SubmenuStyle}">
                                    <Setter Property="Header" Value="{Binding}" />
                                    <Setter Property="ItemsSource" Value="{Binding Children}" />
                                    <Setter Property="HeaderTemplate">
                                        <Setter.Value>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Icon}"
                                                               FontFamily="Segoe MDL2 Assets"
                                                               FontSize="14"
                                                               Foreground="{DynamicResource MenuItemForegroundBrush}"
                                                               Background="Transparent"
                                                               VerticalAlignment="Center"
                                                               Width="16"
                                                               TextAlignment="Center"
                                                               Margin="0,0,8,0"
                                                               Visibility="{Binding Icon, Converter={StaticResource StringToVisibilityConverter}}" />
                                                    <TextBlock Text="{Binding Header}"
                                                               Foreground="{DynamicResource MenuItemForegroundBrush}"
                                                               Background="Transparent"
                                                               VerticalAlignment="Center" />
                                                </StackPanel>
                                            </DataTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="{DynamicResource TitleBarBackgroundBrush}"
            BorderThickness="0">
        <Grid Margin="8,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Logo（这里用纯色块占位，宿主可按需替换为 Image） -->
            <Border Width="24"
                    Height="24"
                    VerticalAlignment="Center"
                    CornerRadius="5"
                    Background="{DynamicResource MenuItemHighlightedBackgroundBrush}" />

            <!-- 标题和菜单 -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="10,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="Lemoo" 
                           Foreground="{DynamicResource TitleBarForegroundBrush}" 
                           FontWeight="SemiBold"
                           FontSize="14"
                           VerticalAlignment="Center"
                           Margin="0,0,20,0" />

                <Menu VerticalAlignment="Center" 
                      Background="Transparent" 
                      Foreground="{DynamicResource TitleBarForegroundBrush}"
                      ItemsSource="{Binding MenuItems}">
                    <Menu.ItemContainerStyle>
                        <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
                            <Setter Property="Header" Value="{Binding}" />
                            <Setter Property="ItemsSource" Value="{Binding Children}" />
                            <EventSetter Event="Click" Handler="MenuItem_Click" />
                            <Setter Property="HeaderTemplate">
                                <Setter.Value>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Header}" />
                                    </DataTemplate>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="ItemContainerStyle">
                                <Setter.Value>
                                    <Style TargetType="MenuItem" BasedOn="{StaticResource SubmenuStyle}">
                                        <Setter Property="Header" Value="{Binding}" />
                                        <Setter Property="ItemsSource" Value="{Binding Children}" />
                                        <EventSetter Event="Click" Handler="MenuItem_Click" />
                                        <Setter Property="HeaderTemplate">
                                            <Setter.Value>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="{Binding Icon}"
                                                                   FontFamily="Segoe MDL2 Assets"
                                                                   FontSize="14"
                                                                   Foreground="{DynamicResource MenuItemForegroundBrush}"
                                                                   Background="Transparent"
                                                                   VerticalAlignment="Center"
                                                                   Width="16"
                                                                   TextAlignment="Center"
                                                                   Margin="0,0,8,0"
                                                                   Visibility="{Binding Icon, Converter={StaticResource StringToVisibilityConverter}}" />
                                                        <TextBlock Text="{Binding Header}"
                                                                   Foreground="{DynamicResource MenuItemForegroundBrush}"
                                                                   Background="Transparent"
                                                                   VerticalAlignment="Center" />
                                                    </StackPanel>
                                                </DataTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Setter Property="ItemContainerStyle">
                                            <Setter.Value>
                                                <Style TargetType="MenuItem" BasedOn="{StaticResource SubmenuStyle}">
                                                    <Setter Property="Header" Value="{Binding}" />
                                                    <Setter Property="ItemsSource" Value="{Binding Children}" />
                                                    <EventSetter Event="Click" Handler="MenuItem_Click" />
                                                    <Setter Property="HeaderTemplate">
                                                        <Setter.Value>
                                                            <DataTemplate>
                                                                <StackPanel Orientation="Horizontal">
                                                                    <TextBlock Text="{Binding Icon}"
                                                                               FontFamily="Segoe MDL2 Assets"
                                                                               FontSize="14"
                                                                               Foreground="{DynamicResource MenuItemForegroundBrush}"
                                                                               Background="Transparent"
                                                                               VerticalAlignment="Center"
                                                                               Width="16"
                                                                               TextAlignment="Center"
                                                                               Margin="0,0,8,0"
                                                                               Visibility="{Binding Icon, Converter={StaticResource StringToVisibilityConverter}}" />
                                                                    <TextBlock Text="{Binding Header}"
                                                                               Foreground="{DynamicResource MenuItemForegroundBrush}"
                                                                               Background="Transparent"
                                                                               VerticalAlignment="Center" />
                                                                </StackPanel>
                                                            </DataTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Menu.ItemContainerStyle>
                </Menu>
            </StackPanel>

            <!-- 占位（用于拖动区域） -->
            <Grid Grid.Column="2" />

            <!-- 系统按钮 -->
            <StackPanel Grid.Column="3"
                        Orientation="Horizontal"
                        VerticalAlignment="Stretch">
                <Button x:Name="MinButton"
                        Width="40"
                        Background="Transparent"
                        BorderThickness="0"
                        Click="MinButton_Click"
                        ToolTip="最小化">
                    <TextBlock Text="&#xE921;" FontFamily="Segoe MDL2 Assets" Foreground="{DynamicResource TitleBarForegroundBrush}" />
                </Button>
                <Button x:Name="MaxRestoreButton"
                        Width="40"
                        Background="Transparent"
                        BorderThickness="0"
                        Click="MaxRestoreButton_Click"
                        ToolTip="最大化/还原">
                    <TextBlock x:Name="MaxRestoreIcon"
                               Text="&#xE922;"
                               FontFamily="Segoe MDL2 Assets"
                               Foreground="{DynamicResource TitleBarForegroundBrush}" />
                </Button>
                <Button x:Name="CloseButton"
                        Width="40"
                        Background="Transparent"
                        BorderThickness="0"
                        Click="CloseButton_Click"
                        ToolTip="关闭">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border"
                                                Background="{TemplateBinding Background}"
                                                BorderThickness="0">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                            VerticalAlignment="Center" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#E81123" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                    <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" Foreground="{DynamicResource TitleBarForegroundBrush}" />
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>

